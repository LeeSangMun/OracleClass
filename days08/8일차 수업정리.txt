[오라클 자료형]
1) CHAR[(size [BYTE|CHAR])]
        기본값 : 1 // CHAR == CHAR(1)
        size 크기의 고정길이 문자 데이터 (1바이트 ~ 2000바이트)
        저장된 문자열의 길이가 다르면 저장공간낭비가 생김 
        그래서 고정된(주민등록, 우편번호 등) 길이의 값을 넣을 때 사용
        CHAR(10 CHAR) // 10문자 

